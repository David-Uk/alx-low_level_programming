#include <stdio.h>

/**
* main - causes an infinite loop
* Return: 0
*/

int main(void)
{
	int num;

	printf("Infinite loop incoming :(\n");

	num = 0;

	/*
	 * while (num < 10)
	 * {
	 * 	putchar(i);
	 * }
	 */
	printf("Infinite loop avoided! \\o/\n");
	return (0);
}
